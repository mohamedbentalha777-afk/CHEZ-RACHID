
export type Language = 'en' | 'fr' | 'ar';

export interface TranslationStrings {
  navHome: string;
  navAbout: string;
  navMenu: string;
  navLocation: string;
  navContact: string;
  heroSlogan: string;
  heroCTA: string;
  aboutTitle: string;
  aboutText: string;
  menuTitle: string;
  locationTitle: string;
  locationDesc: string;
  locationDirections: string;
  locationHighlight: string;
  locationCallToAction: string;
  contactTitle: string;
  contactWhatsApp: string;
  footerRights: string;
}

export type Page = 'home' | 'about' | 'menu' | 'location' | 'contact';
